# Testing Environment Cloud Connections
version: '1.0'

cloudConnections:
  # Test Blob Storage with inline properties
  - displayName: Test Blob Storage
    type: AzureBlobs
    useServicePrincipal: test
    target:
      useTarget: blobStorage.test
    permissions:
      owners:
        - jessica.hill@endjin.com
      users:
        - test.team@endjin.com
      reshareUsers: []

  # Test SQL with a reference to existing connection's service principal
  - displayName: Test SQL Database
    type: SQL
    useServicePrincipal: test
    target:
      useTarget: sqlServer.test
    permissions:
      owners:
        - jessica.hill@endjin.com
      users:
        - test.team@endjin.com
      reshareUsers: []